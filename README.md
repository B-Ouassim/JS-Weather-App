# ☀️ Dynamic Weather App

A responsive web application that fetches and displays current weather conditions for a user-specified city. Built with a focus on clean design and fundamental front-end technologies.

## ✨ Key Features
* **Real-Time Data:** Integrates with the **OpenWeatherMap API** to get up-to-date weather information.
* **Displays:** Temperature, city name, humidity percentage, and wind speed.
* **Dynamic Icons:** Weather icons change based on current conditions (Rain, Clouds, Clear, Drizzle, Mist).
* **Error Handling:** Provides a message for invalid or missing city names.
* **Responsive Design:** Ensures a great user experience on both desktop and mobile devices.

## 💻 Technologies Used
* **HTML5:** Structure and content.
* **CSS3:** Styling, layout, and visual effects (including a `linear-gradient` background).
* **JavaScript (Vanilla):** Core logic, API calls (`fetch`), and DOM manipulation.

## 🚀 Getting Started

1.  **Clone the repository:**
    ```bash
    git clone [https://github.com/YourUsername/JS-Weather-App.git](https://github.com/YourUsername/JS-Weather-App.git)
    ```
2.  **API Key Setup (Required for full functionality):**
    * Get a free API key from [OpenWeatherMap](https://openweathermap.org/api).
    * Replace the placeholder value for `apiKey` in your `main.js` file with your actual key.
3.  **Launch:** Open the `index.html` file in your web browser.

## 💡 How to Use
1.  Enter the name of any major city into the search bar.
2.  Click the search button or press Enter to view the current weather.
